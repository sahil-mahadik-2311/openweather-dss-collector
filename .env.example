# Copy to .env for local runs. On Render, set these in the dashboard instead.
OPENWEATHER_API_KEY=your_key_here

# Where CSVs are written. On Render this must be the persistent disk mount path.
DATA_DIR=./data

# Optional collection window in IST. Leave blank to collect around the clock.
COLLECT_START=
COLLECT_END=
